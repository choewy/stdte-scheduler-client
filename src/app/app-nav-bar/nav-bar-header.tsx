import { FC } from 'react';
import { Typography } from '@mui/material';

export const AppNavBarHeader: FC = () => {
  return (
    <Typography variant="h6" component="div" sx={{ flexGrow: 1 }}>
      STDTE
    </Typography>
  );
};
